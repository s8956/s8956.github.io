SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
HOME=/root

* * * * * root for i in {1..6}; do "${HOME}/apps/asterisk/app.asterisk.room_close.sh" > /dev/null 2>&1 & sleep 10; done
