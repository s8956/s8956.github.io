SHELL=/bin/bash
PATH=/bin:/sbin:/usr/bin:/usr/sbin:/usr/local/bin:/usr/local/sbin
HOME=/root

* * * * * root for i in {1..6}; do "${HOME}/apps/asterisk/app.asterisk.room-close.sh" > /dev/null 2>&1 & sleep 10; done
